package org.stockmanager.storage;

/**
 * Provides classes and interfaces for managing data storage and retrieval
 * within the stock management application. This package abstracts and
 * handles interactions with the underlying data persistence layer.
 *
 * <p>The {@code storage} package is responsible for:</p>
 * <ul>
 *   <li>{@link org.stockmanager.storage.DataStorage} - Provides simple in-memory storage for user data,
 *       portfolios, and transactions</li>
 * </ul>
 *
 * <p>By centralizing the application's storage-related logic here, this package
 * supports scalability, maintainability, and flexibility for data management needs.</p>
 *
 * @see org.stockmanager.storage.DataStorage
 */